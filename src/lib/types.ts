// Utility types used in the project

export interface HeaderMenuItem {
    text: string,
    target: string
}

export interface Project {
    title: string,
    context: string,
    description: string
}
